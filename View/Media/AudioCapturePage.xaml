<Page
    x:Class="TrustworthyCompanion.View.Media.AudioCapturePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TrustworthyCompanion.View.Admin.Question"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:i="using:Microsoft.Xaml.Interactivity"
	xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
	DataContext="{Binding AudioCaptureViewModel, Source={StaticResource ViewModelLocator}}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<i:Interaction.Behaviors>
		<core:EventTriggerBehavior EventName="Loaded">
			<core:InvokeCommandAction Command="{Binding PageLoadedCommand}" />
		</core:EventTriggerBehavior>
		<core:EventTriggerBehavior EventName="Unloaded">
			<core:InvokeCommandAction Command="{Binding PageUnloadedCommand}" />
		</core:EventTriggerBehavior>
	</i:Interaction.Behaviors>
	
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<MediaElement x:Name="Audio" Source="{Binding Path=Question.AudioFile}" Visibility="Collapsed" AutoPlay="False" />

		<ToggleButton Grid.Row="0" Content="Record Audio" Command="{Binding Path=RecordAudioCommand}" CommandParameter="{Binding}" HorizontalAlignment="Center" IsEnabled="{Binding Path=RecordEnabled}"/>
		<Button Grid.Row="1" Command="{Binding Path=PlayAudioCommand}" IsEnabled="{Binding Path=PlayEnabled}" CommandParameter="{Binding ElementName=Audio}" Content="Play" HorizontalAlignment="Center" />
	</Grid>


	<Page.BottomAppBar>
		<CommandBar>
			<CommandBar.PrimaryCommands>
				<AppBarButton Content="Delete" Command="{Binding DeleteAudioCommand}" Icon="Delete" Label="Delete" />
			</CommandBar.PrimaryCommands>
		</CommandBar>
	</Page.BottomAppBar>
</Page>
